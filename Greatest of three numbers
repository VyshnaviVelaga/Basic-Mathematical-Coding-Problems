class Solution {
  public:
    int greatestOfThree(int A, int B, int C) {
        // code here
        int greatest = A;
        if (B > greatest) {
            greatest = B;
        }
        if (C > greatest) {
            greatest = C;
        }
        return greatest;
    }
};
